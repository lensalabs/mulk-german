---
// Shared Reward Banner component — extracted from dashboard and settings (#5)
// Now dismissible: shows once, then hidden forever (stored in localStorage)
interface Props {
  variant?: 'compact' | 'full';
}
const { variant = 'compact' } = Astro.props;
---

<div id="reward-banner" class="card bg-gradient-to-br from-[var(--color-primary)]/5 to-[var(--color-accent)]/10 border-[var(--color-primary)]/20 hidden">
  {variant === 'compact' ? (
    <div class="flex items-start gap-3">
      <div class="w-11 h-11 rounded-xl bg-[var(--color-primary)]/10 flex items-center justify-center shrink-0">
        <svg class="w-5 h-5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"></path></svg>
      </div>
      <div class="flex-1 min-w-0">
        <h3 class="heading-card text-sm">Schließe die Challenge ab — gewinne einen Quran!</h3>
        <p class="text-xs text-[var(--color-text-secondary)] mt-1 leading-relaxed">Wer alle 30 Tage erfolgreich abschließt, erhält als Geschenk ein Exemplar des Heiligen Quran.</p>
        <a href="/app/gift" class="group inline-flex items-center gap-1 text-xs font-medium text-[var(--color-primary)] mt-2">
          Mehr erfahren
          <svg class="w-3 h-3 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
        </a>
      </div>
      <button id="dismiss-banner" class="w-8 h-8 rounded-lg flex items-center justify-center text-[var(--color-text-secondary)] hover:bg-[var(--color-background)] shrink-0 -mt-1 -mr-1" aria-label="Schließen">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
  ) : (
    <Fragment>
      <div class="flex items-center justify-between mb-4">
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 rounded-lg bg-[var(--color-primary)]/10 flex items-center justify-center">
            <svg class="w-4 h-4 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"></path></svg>
          </div>
          <h2 class="heading-card">Das Challenge-Geschenk</h2>
        </div>
        <button id="dismiss-banner" class="w-8 h-8 rounded-lg flex items-center justify-center text-[var(--color-text-secondary)] hover:bg-[var(--color-background)]" aria-label="Schließen">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M6 18L18 6M6 6l12 12"/></svg>
        </button>
      </div>
      <div class="space-y-3 text-sm text-[var(--color-text-secondary)] leading-relaxed">
        <p>Jeder Teilnehmer, der alle <strong class="text-[var(--color-text)]">30 Tage der Challenge</strong> erfolgreich abschließt, erhält als Geschenk:</p>
        <div class="flex items-center gap-3 bg-[var(--color-surface)] rounded-2xl p-4 border border-[var(--color-border)]">
          <div class="w-10 h-10 rounded-xl bg-teal-600/10 flex items-center justify-center shrink-0">
            <svg class="w-5 h-5 text-teal-700" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
          </div>
          <div>
            <p class="font-semibold text-[var(--color-text)]">Ein Exemplar des Heiligen Quran</p>
            <p class="text-xs text-[var(--color-text-secondary)] mt-0.5">Kostenloser Versand nach Abschluss der Challenge</p>
          </div>
        </div>
        <p class="text-xs">Die Challenge beginnt mit Ramadan. Jeden Tag lernst du einen Vers aus Sure Al-Mulk: Hören, Lesen und Auswendiglernen.</p>
        <a href="/app/gift" class="inline-flex items-center gap-1 text-xs font-medium text-[var(--color-primary)] mt-2">
          Geschenk-Details ansehen
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
        </a>
      </div>
    </Fragment>
  )}
</div>

<script>
  // Show banner only if not dismissed
  const STORAGE_KEY = 'mulk30_banner_dismissed';
  const banner = document.getElementById('reward-banner');
  const dismissBtn = document.getElementById('dismiss-banner');
  
  if (banner) {
    const isDismissed = localStorage.getItem(STORAGE_KEY) === 'true';
    if (!isDismissed) {
      banner.classList.remove('hidden');
    }
  }
  
  dismissBtn?.addEventListener('click', () => {
    localStorage.setItem(STORAGE_KEY, 'true');
    banner?.classList.add('hidden');
  });
</script>
